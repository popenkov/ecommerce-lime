import styled from "styled-components";

const Container = styled.div`
  position: relative;
`;

const IconContainer = styled.div`
  position: absolute;
  right: 7px;
  top: 7px;
`;

export const Styled = {
  Container,
  IconContainer,
};
